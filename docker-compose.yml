registry:
  container_name: cli-registry
  restart: always
  image: registry:2
  ports:
    - 5000:5000

server:
    image: ${MERKELY_IMAGE_URI}
    container_name: merkely-server
    ports: 
      - 8001:8001 
